<div class="nhsapp-card app-card--dark-blue">
  <div class="nhsapp-card__content app-card__content">
    <h2 class="nhsapp-card__heading nhsuk-heading-m nhsuk-u-padding-top-2">{{ params.title }}</h2>
    <p>{{ params.description }}</p>
    <a class="nhsuk-button nhsuk-button--reverse app-button" href="{{ params.URL }}">
      {{ params.linkText }}
      {%- if params.hiddenText %}<span class="nhsuk-u-visually-hidden">{{ params.hiddenText }}</span>{% endif %}
    </a>
  </div>
</div>